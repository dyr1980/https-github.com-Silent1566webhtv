name: Android Release

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Release tag, blank = v<versionName>-yyyyMMddHHmm or v<versionName>-beta-yyyyMMddHHmm"
        required: false
        type: string
      release_channel:
        description: "Release channel, auto uses beta on dev/fongmi-sync and stable on main"
        required: false
        default: auto
        type: choice
        options:
          - auto
          - stable
          - beta
      release_notes:
        description: "Release notes, use \\n for Markdown newlines"
        required: false
        type: string
      prerelease:
        description: "Mark as pre-release"
        required: false
        default: false
        type: boolean
      sync_cnb:
        description: "Optionally mirror APKs to CNB Release and sync CNB manifests"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: android-release-update-channel
  cancel-in-progress: false

env:
  TZ: Asia/Shanghai
  CNB_API_ENDPOINT: https://api.cnb.cool
  CNB_WEB_ENDPOINT: https://cnb.cool
  CNB_REPO_SLUG: fish2035/webhtv-release
  CNB_REPO_URL: https://cnb.cool/fish2035/webhtv-release.git
  UPDATE_CHANNEL_TAG: update-channel

jobs:
  release:
    name: Build and publish
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Verify bundled MPV native assets
        run: bash scripts/verify_mpv_native_assets.sh --require-elf

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android packages
        run: |
          # API 37 is not published in sdkmanager's stable channel; install the platform archive directly.
          sdkmanager "platform-tools" "platforms;android-28" "build-tools;36.0.0"
          curl -fsSL -o /tmp/platform-37.zip https://dl.google.com/android/repository/platform-37.0_r01.zip
          rm -rf /tmp/platform-37 "$ANDROID_HOME/platforms/android-37"
          mkdir -p /tmp/platform-37 "$ANDROID_HOME/platforms"
          unzip -q /tmp/platform-37.zip -d /tmp/platform-37
          platform_jar="$(find /tmp/platform-37 -name android.jar -print -quit)"
          test -n "$platform_jar"
          mv "$(dirname "$platform_jar")" "$ANDROID_HOME/platforms/android-37"

      - name: Resolve release tag
        id: meta
        run: |
          VERSION_NAME="$(sed -n 's/^[[:space:]]*versionName[[:space:]]*"\([^"]*\)".*/\1/p' app/build.gradle | head -1)"
          VERSION_CODE="$(sed -n 's/^[[:space:]]*versionCode[[:space:]]*\([0-9][0-9]*\).*/\1/p' app/build.gradle | head -1)"
          CHANNEL="${{ inputs.release_channel }}"
          if [ "$CHANNEL" = "auto" ] || [ -z "$CHANNEL" ]; then
            if [ "${{ github.ref_name }}" = "dev" ] || [ "${{ github.ref_name }}" = "fongmi-sync" ]; then
              CHANNEL="beta"
            else
              CHANNEL="stable"
            fi
          fi
          COMMIT_EPOCH="$(git show -s --format=%ct "$GITHUB_SHA")"
          TAG_TIME="$(date -d "@$COMMIT_EPOCH" +%Y%m%d%H%M)"
          if [ "$CHANNEL" = "beta" ]; then
            APK_SUFFIX="-beta"
            TAG_PATTERN="^v${VERSION_NAME}-beta-[0-9]{12}$"
            TAG_DEFAULT="v${VERSION_NAME}-beta-$TAG_TIME"
            TAG_EXPECT="v${VERSION_NAME}-beta-yyyyMMddHHmm"
          else
            APK_SUFFIX=""
            TAG_PATTERN="^v${VERSION_NAME}-[0-9]{12}$"
            TAG_DEFAULT="v${VERSION_NAME}-$TAG_TIME"
            TAG_EXPECT="v${VERSION_NAME}-yyyyMMddHHmm"
          fi
          TAG="${{ inputs.release_tag }}"
          if [ -z "$TAG" ]; then
            TAG="$TAG_DEFAULT"
          fi
          if ! printf '%s' "$TAG" | grep -Eq "$TAG_PATTERN"; then
            echo "::error title=Invalid release tag::Expected $TAG_EXPECT, got: $TAG"
            exit 1
          fi
          if EXISTING_TAG_SHA="$(git rev-parse "$TAG^{commit}" 2>/dev/null)"; then
            if [ "$EXISTING_TAG_SHA" != "$GITHUB_SHA" ]; then
              echo "::error title=Existing release tag targets another commit::$TAG points to $EXISTING_TAG_SHA, expected $GITHUB_SHA"
              exit 1
            fi
            echo "::notice title=Reusing release tag::$TAG already points to this commit"
          fi
          {
            echo "tag=$TAG"
            echo "title=${TAG#v}"
            echo "version_name=$VERSION_NAME"
            echo "version_code=$VERSION_CODE"
            echo "short_sha=$(git rev-parse --short=7 HEAD)"
            echo "channel=$CHANNEL"
            echo "apk_suffix=$APK_SUFFIX"
          } >> "$GITHUB_OUTPUT"

      - name: Configure release signing
        run: |
          {
            echo "storeFile=$GITHUB_WORKSPACE/app/signing/my_release_key.jks"
            echo "keyAlias=myapp_key"
            echo "storePassword=123456"
            echo "keyPassword=123456"
          } > local.properties
          echo "Release signing configured"

      - name: Build four release APKs
        env:
          WEBHTV_RELEASE_TAG: ${{ steps.meta.outputs.tag }}
          WEBHTV_APK_SUFFIX: ${{ steps.meta.outputs.apk_suffix }}
        run: |
          chmod +x ./gradlew
          rm -rf Release/apk
          ./gradlew \
            :app:assembleMobileArm64_v8aRelease \
            :app:assembleMobileArmeabi_v7aRelease \
            :app:assembleLeanbackArm64_v8aRelease \
            :app:assembleLeanbackArmeabi_v7aRelease \
            --no-daemon

      - name: Collect artifacts
        run: |
          rm -rf dist
          mkdir -p dist
          cp Release/apk/*.apk dist/
          ls -lh dist/

      - name: Prepare release notes input
        id: release_notes
        env:
          RELEASE_NOTES_RAW: ${{ inputs.release_notes }}
        run: |
          python3 - <<'PY'
          import os

          raw = os.environ.get("RELEASE_NOTES_RAW", "").strip()
          notes = (
              raw
              .replace("\\r\\n", "\n")
              .replace("\\n", "\n")
              .replace("\\t", "\t")
              .replace('\\"', '"')
              .replace("\\'", "'")
          )
          delimiter = "RELEASE_NOTES_EOF"
          while delimiter in notes:
              delimiter += "_"
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as fh:
              fh.write(f"has_notes={'true' if notes else 'false'}\n")
              fh.write(f"notes<<{delimiter}\n")
              fh.write(notes)
              if notes and not notes.endswith("\n"):
                  fh.write("\n")
              fh.write(f"{delimiter}\n")
          PY

      - name: Generate update manifests
        env:
          RELEASE_NOTES: ${{ steps.release_notes.outputs.notes }}
          GITHUB_RELEASE_DOWNLOAD_BASE: https://github.com/${{ github.repository }}/releases/download/${{ steps.meta.outputs.tag }}
        run: |
          python3 - <<'PY'
          import glob
          import hashlib
          import json
          import os

          tag = "${{ steps.meta.outputs.tag }}"
          notes = os.environ.get("RELEASE_NOTES", "").strip()
          release_download_base = os.environ["GITHUB_RELEASE_DOWNLOAD_BASE"]
          for apk in glob.glob("dist/*.apk"):
              base = os.path.splitext(os.path.basename(apk))[0]
              with open(apk, "rb") as fh:
                  content = fh.read()
              data = {
                  "name": tag,
                  "desc": f"{tag} - ${{ steps.meta.outputs.short_sha }}",
                  "channel": "${{ steps.meta.outputs.channel }}",
                  "code": int("${{ steps.meta.outputs.version_code }}"),
                  "apk": f"{release_download_base}/{base}.apk",
                  "size": len(content),
                  "sha256": hashlib.sha256(content).hexdigest(),
              }
              if notes:
                  data["notes"] = notes
              with open(f"dist/{base}.json", "w", encoding="utf-8") as fh:
                  json.dump(data, fh, ensure_ascii=False, indent=2)
                  fh.write("\n")
          PY
          ls -lh dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          name: ${{ steps.meta.outputs.title }}
          target_commitish: ${{ github.sha }}
          body: ${{ steps.release_notes.outputs.notes != '' && steps.release_notes.outputs.notes || '手动触发 GitHub Actions 构建发布。' }}
          prerelease: ${{ inputs.prerelease == true || steps.meta.outputs.channel == 'beta' }}
          overwrite: true
          files: |
            dist/*.apk
            dist/*.json

      - name: Publish update channel manifests (curl GitHub API direct call)
        env:
          GH_TOKEN: ${{ github.token }}
          API_BASE: https://api.github.com/repos/${{ github.repository }}
          UPLOAD_BASE: https://uploads.github.com/repos/${{ github.repository }}
          UPDATE_CHANNEL_TAG: ${{ env.UPDATE_CHANNEL_TAG }}
          CURRENT_CHANNEL: ${{ steps.meta.outputs.channel }}
        run: |
          set -euo pipefail
          rm -rf channel-manifests other-channel-manifests
          mkdir -p channel-manifests other-channel-manifests
          cp dist/*.json channel-manifests/

          # 1. 查询update‑channel是否存在
          query_release=$(curl -s -w "\n%{http_code}" \
            -H "Authorization: token $GH_TOKEN" \
            "$API_BASE/releases/tags/$UPDATE_CHANNEL_TAG")
          http_code=$(echo "$query_release" | tail -n1)
          release_body=$(echo "$query_release" | head -n -1)

          if [[ "$http_code" == "404" ]]; then
            echo "update‑channel release不存在，调用API创建"
            curl -s -X POST \
              -H "Authorization: token $GH_TOKEN" \
              -H "Content-Type: application/json" \
              "$API_BASE/releases" \
              -d "$(jq -n \
                --arg tag "$UPDATE_CHANNEL_TAG" \
                --arg sha "$GITHUB_SHA" \
                '{
                  tag_name: $tag,
                  target_commitish: $sha,
                  name:"Update‑channel manifests",
                  body:"Machine‑managed stable and beta update manifests.",
                  prerelease:true,
                  draft:false
                }')"
            query_release=$(curl -s -w "\n%{http_code}" \
              -H "Authorization: token $GH_TOKEN" \
              "$API_BASE/releases/tags/$UPDATE_CHANNEL_TAG")
            http_code=$(echo "$query_release" | tail -n1)
            release_body=$(echo "$query_release" | head -n -1)
          fi

          # 解析release_id 和已有asset列表
          release_id=$(echo "$release_body" | jq -r '.id')
          asset_count=$(echo "$release_body" | jq '.assets | length')
          echo "update‑channel release_id=$release_id, assets=$asset_count"

          # 遍历现有asset，下载需要继承的manifest json
          echo "$release_body" | jq -c '.assets[]' | while read -r asset; do
            asset_name=$(echo "$asset" | jq -r '.name')
            asset_browser_url=$(echo "$asset" | jq -r '.browser_download_url')
            if [[ "$asset_name" != *.json ]]; then
              continue
            fi
            # 跳过当前渠道旧清单，保留另一渠道
            if [[ "$CURRENT_CHANNEL" == "beta" && "$asset_name" == *-beta.json ]]; then
              echo "skip old beta manifest: $asset_name"
              continue
            fi
            echo "download inherit manifest $asset_name"
            curl -sL -o "other-channel-manifests/$asset_name" "$asset_browser_url"
          done

          # 复制继承过来的旧manifest
          for f in other-channel-manifests/*; do
            [[ -f "$f" ]] || continue
            cp "$f" channel-manifests/
          done

          # 删除update‑channel全部旧json assets
          echo "$release_body" | jq -c '.assets[]' | while read -r asset; do
            asset_name=$(echo "$asset" | jq -r '.name')
            asset_id=$(echo "$asset" | jq -r '.id')
            if [[ "$asset_name" != *.json ]]; then
              continue
            fi
            echo "delete old asset $asset_name id=$asset_id"
            curl -s -X DELETE \
              -H "Authorization: token $GH_TOKEN" \
              "$API_BASE/releases/assets/$asset_id"
          done

          # curl API上传全部新channel‑manifests/*.json
          for json_file in channel-manifests/*.json; do
            base=$(basename "$json_file")
            echo "upload $base"
            curl -s -X POST \
              -H "Authorization: token $GH_TOKEN" \
              -H "Content-Type: application/json" \
              --data-binary "@$json_file" \
              "$UPLOAD_BASE/releases/$release_id/assets?name=$base"
          done
          echo "✅ update‑channel manifests 全部完成，纯curl api调用，无gh download"

      - name: Sync to CNB
        if: ${{ inputs.sync_cnb }}
        env:
          CNB_TOKEN: ${{ secrets.CNB_TOKEN }}
          CNB_RELEASE_TAG: ${{ steps.meta.outputs.tag }}
          CNB_RELEASE_TITLE: ${{ steps.meta.outputs.title }}
          CNB_RELEASE_NOTES: ${{ steps.release_notes.outputs.notes }}
          CNB_RELEASE_PRERELEASE: ${{ inputs.prerelease == true || steps.meta.outputs.channel == 'beta' }}
          CNB_RELEASE_LATEST: ${{ steps.meta.outputs.channel == 'stable' }}
        run: |
          .github/scripts/sync-cnb-release.sh

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: webhtv-${{ steps.meta.outputs.tag }}
          path: dist/*
          if-no-files-found: error
          retention-days: 30
